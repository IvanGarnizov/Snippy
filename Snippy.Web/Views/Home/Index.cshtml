@using Snippy.Web.Models.ViewModels
@model HomeViewModel

<div class="col-lg-5">
    <h1>Latest snippets:</h1>

    @Html.DisplayFor(m => m.Snippets)
</div>

<div class="col-lg-5">
    <h1>Latest comments:</h1>

    @Html.DisplayFor(m => m.Comments)
</div>

<div class="col-lg-2">
    <div class="best-labels">
        <div id="logo">Best labels</div>

        @Html.DisplayFor(m => m.Labels)
    </div>
</div>